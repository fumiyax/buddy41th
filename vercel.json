{
  "buildCommand": "echo 'No build needed'",
  "installCommand": "echo 'No install needed'",
  "rewrites": [
    { "source": "/:path*", "destination": "/referee/:path*" }
  ],
  "functions": {
    "api/**/*.js": {
      "maxDuration": 10
    }
  }
}
